# مصحف الركوع

تطبيق ويب (PWA) لقراءة القرآن الكريم وفق تقسيم الركوع.

يقسّم المصحف الشريف كاملًا إلى **٥٥٦ صفحة**، كل صفحة تمثّل ركوعًا واحدًا كاملًا حسب علامات الركوع (ع) الموجودة في المصحف، من الفاتحة إلى الناس. النص Uthmani من مصادر Tanzil / QPC (رواية حفص عن عاصم).

يعمل بالكامل بدون إنترنت بعد أول فتح (PWA)، وفيه شاشة رئيسية تعرض تقدّم القراءة، وفهرس للسور، وفهرس للأجزاء، وبحث، ومفضّلة لحفظ أي ركوع باسم مخصّص، وضبط لحجم الخط، ووضع ليلي.

---

## ✅ حالة المشروع

هذا الإصدار مستقر وجاهز للاستخدام الرسمي (Stable Release).

يستمر العمل على تحسينات مستقبلية مثل ورد اليوم، والإحصائيات، وتوسيع التلاوة الصوتية، لكنها تُضاف كإصدارات لاحقة فوق قاعدة مستقرة، لا كجزء من إصدار غير مكتمل.

نرحب بجميع الاقتراحات والملاحظات والمساهمات.

---

## الاختبارات

### اختبارات الوحدة والانحدار (Node)

```bash
node tests/<file>.js
# أو بعد تثبيت التبعيات:
npm run test:unit
```

موجودة في مجلد `tests/` وتغطي المنطق (نسخ احتياطي، بحث، TTS، صلاة، QCF، …).

### اختبارات المتصفح الحقيقي (Playwright E2E)

تغطي السيناريوهات التي لا تغطيها اختبارات الوحدة:

- فتح التطبيق و Offline
- الانتقال بين الركوعات
- البحث
- تغيير الخط والوضع الليلي
- التلاوة
- RTL، Service Worker، رفض صلاحيات الموقع

```bash
npm install
npx playwright install chromium
npm run test:e2e
```

التفاصيل في [`e2e/README.md`](./e2e/README.md).

---

## الإصدار الحالي

**الإصدار:** 1.0.671 — الإصدار الرسمي المستقر (Stable Release)

رقم الإصدار مصدره الوحيد ملف [`version.js`](./version.js) — يُقرأ من هناك في الإعدادات/About داخل التطبيق، ويُشتق منه اسم كاش الـ Service Worker (`sw.js` عبر `importScripts`) حتى لا يختلف الرقم بين هذا الملف والتطبيق نفسه كما كان يحدث سابقًا. حقل `version` في `manifest.json` غير قابل للقراءة تلقائيًا من JS (ملف JSON ساكن) لذا يجب تحديثه يدويًا مع كل تغيير في `version.js`؛ `app.js` يتحقق من التطابق عند التشغيل ويطبع تحذيرًا في console عند أي اختلاف.

عند إصدار نسخة جديدة: حدّث الرقم في `version.js` وفي حقل `version` بملف `manifest.json` معًا (وحدّث هذا الملف إن رغبت).

**الحالة:**
- ✅ يعمل
- ✅ مستقر (Stable)

---

## سجل التغييرات (Changelog)

يتّبع هذا المشروع ترقيمًا مبسّطًا (Semantic-ish): `major.minor.patch`. لم يكن هناك سجل تغييرات موثّق قبل هذا القسم، فالإصلاحات الأقدم مذكورة هنا بإيجاز تحت "قبل بدء هذا السجل" بلا أرقام إصدار دقيقة؛ بداية من 0.9.1 كل تغيير له رقم إصدار وتاريخ.

**ملاحظة:** الإصدار الحالي الرسمي المستقر هو **1.0.671** (مطابق لما هو مذكور في `version.js` و`manifest.json` وأعلى هذا الملف). الإدخالات أدناه هي السجل الموثّق فقط؛ لا توجد إدخالات موثّقة للإصدارات بين 1.0.299 و1.0.486، ولا بين 1.0.516 و1.0.636، ولا بين 1.0.658 و1.0.664 (راجع `PROJECT_STATUS.md` لتفاصيل غير موثّقة هنا).

### 1.0.671 — 2026-09-08
- اعتماد تلوين مواضع 1–15 عدا 4 (6:141:25) و11 (12:29:6).

### 1.0.670 — 2026-09-08
- إعادة نشر بعد بلاغ عدم تلوين 22:36:9؛ تأكيد وجود ق في الخريطة + تحديث رقم الإصدار لكسر الكاش.

### 1.0.669 — 2026-09-08
- اعتماد تلوين مواضع 16–33 من قائمة الوقف المرجّح (≥1 قارئ) بدرجات السجاوندي.

### 1.0.668 — 2026-09-08
- 57:19:7 الصديقون: اعتماد ق (أخضر).
- 57:19:10 ربهم: حذف تلوين ط (مخالفة قراءة القراء).

### 1.0.667 — 2026-09-08
- تلوين 5 مواضع اتفق القراء الأربعة على وقف واضح عندها (تحليل تقديري): 33:19:2 ج، 33:23:15 ز، 38:5:4 ج، 38:65:4 ق، 42:31:5 ج.

### 1.0.666 — 2026-09-07
- استثناء 12 موضعًا (صلي في النسخ + علامة مدينة غير صلي ۚ) واعتماد تلوين درجة وقف النسخ في مصحف المدينة.
- ج بني: 2:236:13، 3:167:3، 10:109:8، 33:44:4
- ز/ق أخضر: 8:69:5، 21:45:4، 3:193:10، 13:35:16، 14:40:6، 18:18:4، 18:64:5، 60:1:32
- اختبار انحدار: `tests/sila-override-batch-666-regression.js` (50/50).

### 1.0.665 — 2026-09-07
- استثناء موثّق لتقاطع ز×صلي عند 29:60:9 (العنكبوت، «وَإِيَّاكُمۡ») — يحمل الموضع علامتي صلي وز معًا في مصحف النسخ؛ اعتماد ز (تلوين أخضر) عبر إدخال في `DEFAULT_MARK_LA_SILA_OVERRIDES` مع حذف الاستبعاد اليدوي المقابل من `DEFAULT_MARK_MANUAL_EXCLUSIONS['ZAY_JAWAZ']`. تفاصيل كاملة في `PROJECT_STATUS.md`.

### 1.0.657 — 2026-09-06
- إصلاح Data Bug في `DEFAULT_MARK_MANUAL_ADDITIONS['JEEM']`: حذف المدخل الخاطئ `'59:7:23'` (كلمة «مِنكُمۡ» في الحشر 59:7).
- الموضع مصنَّف أصلاً TA_MUTLAQ في `WAQF_POSITIONS` (المصدر)؛ الإضافة اليدوية الخاطئة لـ JEEM على نفس الكلمة كانت تجعلها تُعرض باللون البني بدل الأزرق رغم أن Long Press كان يعرض التصنيف الصحيح (ط) من المصدر مباشرة.
- لا تغيير في `WAQF_POSITIONS`، CSS، منطق Long Press، أو أي موضع آخر في الجدول — تعديل سطر واحد فقط.
- اختبار انحدار يدوي: تحقّق مباشر عبر `buildDefaultWordMap('TA_MUTLAQ'|'JEEM'|'QAD_QILA'|'WAQF_LAZIM')` على 59:7 يؤكد أن كل علامة من العلامات الخمس أصبحت بتصنيف واحد فقط دون تعارض.

### 1.0.656 — 2026-09-06
- بوصلة القبلة: مغادرة تاب الصلاة = إغلاق كامل (`stopCompass`) وليس Pause/Resume.
- إزالة `COMPASS_TAB_RESUME_WINDOW_MS` و`compassPausedAt` ومسار `startCompass({ resume: true })`.
- العودة إلى تاب الصلاة لا تعيد تشغيل البوصلة؛ الفتح يدوي فقط عبر «فتح البوصلة».
- اختبار انحدار: `tests/compass-tab-lifecycle-regression.js`.

### 1.0.655 — 2026-09-06
- بوصلة القبلة: إغلاق تلقائي عند مغادرة تاب الصلاة أو إغلاق دليل القارئ بأي مسار (بما فيه زر الرجوع في النظام)، عبر مراقبة إخفاء اللوحة واستدعاء `Prayer.onTabHidden`.

### 1.0.654 — 2026-09-06
- تفسير الركوع: فصل preload نص التفسير عن CATT warm-up.
- `prefetchTafsirTexts`: تحميل نص current/next/prev بشكل متوازٍ ومستقل عن CATT.
- الانتقال للتالي/السابق يعرض من الكاش مباشرة بدون skeleton عندما يكون النص جاهزًا.
- CATT يبقى في الخلفية ولا يمنع عرض النص.

### 1.0.653 — 2026-09-06
- تفسير الركوع: عرض فوري من الكاش (`renderCachedRuku`) عند اكتمال تحميل آيات الركوع.
- مسار سريع في `loadCurrentRuku` عند `allAyahsCached`.

### 1.0.652 — 2026-09-06
- تصحيح توثيق `tests/prayer-offline-precache-regression.js` ليعكس أن `prayer.js` sync + precache (وليس Lazy).

### 1.0.652 — 2026-09-06
- توحيد توثيق اختبار `prayer-offline-precache-regression`: prayer.js sync في index.html + safeInit (وليس Lazy-loaded).

### 1.0.651 — 2026-09-06
- إصلاح تشغيل الإذاعة بعد إعادة الضبط: منع ربط مستمع الزر مرتين، وعدم استدعاء `reloadFromStorage` عند كل فتح للتاب، و precache لـ `radio-player.js`.

### 1.0.650 — 2026-09-06
- تعزيز إعادة ضبط الإذاعة: فرض «القرآن الكريم من القاهرة» بعد Factory Reset حتى مع التحميل الكسول، وإعادة قراءة التخزين عند كل فتح لتبويب الإذاعة.

### 1.0.649 — 2026-09-06
- بعد إعادة ضبط التطبيق تعود محطة الإذاعة الافتراضية إلى «القرآن الكريم من القاهرة» (`reloadFromStorage` + حفظ `stationId` في `RADIO_KEY`).

### 1.0.648 — 2026-09-05
- إصلاح تاب الصلاة: إعادة `prayer.js` للتحميل مع الصفحة + `rebuildLocationSelect()` داخل `init` — تظهر المدن والمواقع المحفوظة وتُربَط البوصلة بعد التهيئة.

### 1.0.647 — 2026-09-05
- إصلاح التفسير بعد مسح بيانات التصفح: إعادة `reader-tafsir.js` للتحميل المتزامن مع الصفحة (لا يعتمد على lazy fetch)، مع الإبقاء على تأجيل قاموس التشكيل و`tts-diacritizer` في الخلفية.

### 1.0.646 — 2026-09-05
- إصلاح «تعذّر تحميل التفسير»: تحميل `reader-tafsir.js` وحده لفتح اللوحة، وتأجيل قاموس التشكيل/`tts-diacritizer` للخلفية؛ فتح مباشر عبر `ReaderTafsir.openPanel()`؛ precache لـ `reader-tafsir.js`؛ تحسين مسارات LazyLoader.

### 1.0.645 — 2026-09-05
- تعزيز `e2e/advanced-scenarios.spec.js`: assertions حقيقية لحفظ الخط (fontStyle في `juzamma_v1` + كلاس body + زر active)، وظهور قائمة long-press، وتسجيل SW، بدل فحوصات `typeof`/عدم الانهيار فقط.

### 1.0.644 — 2026-09-05
- تحسين `e2e/prayer-offline.spec.js`: انتظار ظهور `prayer.js` في Cache Storage بدل `waitForTimeout(2500)` الثابت.

### 1.0.643 — 2026-09-05
- إصلاح offline regression: إعادة `prayer.js` إلى قائمة precache في Service Worker (`DYNAMIC_ASSETS`) مع الإبقاء على التحميل الكسول في الصفحة — الصلاة/القبلة Core offline حتى قبل أول فتح للتبويب.
- إضافة `tests/prayer-offline-precache-regression.js` و`e2e/prayer-offline.spec.js`.
- تصحيح assertion ضعيفة في اختبار Service Worker (`|| true`).

### 1.0.642 — 2026-09-05
- تحسين سرعة التشغيل الأول: تحميل كسول لوحدات الإذاعة (`radio-player.js`) ومواقيت الصلاة (`prayer.js`) والتفسير (`reader-tafsir.js` + قاموس التشكيل + `tts-diacritizer.js`) عند أول استخدام فقط عبر `lazy-loader.js`.
- إخراج الوحدات غير-Core من قائمة `cache.addAll` في تثبيت Service Worker؛ `prayer.js` أُعيد إلى precache في 1.0.652.
- لا تغيير في منطق الميزات نفسها بعد التحميل.

### 1.0.641 — 2026-09-05
- إضافة طبقة اختبارات متصفح حقيقية (Playwright E2E) في مجلد `e2e/`: السيناريوهات الأساسية (فتح التطبيق، الانتقال لركوع، البحث، تغيير الخط/الوضع الليلي، النسخ الاحتياطي، التلاوة، offline) بالإضافة إلى سيناريوهات متقدمة (RTL، ضغط مطوّل، Service Worker، رفض صلاحية الموقع).
- إضافة `package.json` و`playwright.config.js` ووثائق التشغيل في `e2e/README.md`.
- لا تغييرات وظيفية في منطق التطبيق نفسه.

### 1.0.640 — 2026-09-05
- توحيد معلومات الإصدار والتوثيق: إزالة ملاحظة قديمة في هذا الملف كانت تذكر 1.0.515 كإصدار حالي رغم وصول المشروع لـ 1.0.639؛ وتصحيح `PROJECT_STATUS.md` الذي كان يحمل ثلاثة أرقام إصدار متضاربة فيه (1.0.627 في الرأس، 1.0.174 في قسم Project Metrics، وإدخال 1.0.628 مُقحَم داخل سجل قديم). لا تغييرات وظيفية في التطبيق نفسه.

### 1.0.639 — 2026-09-05
- نافذة إرشادات معايرة البوصلة: إضافة بند أول جديد «معايرة البوصلة في مكان مفتوح.» قبل بند تفعيل GPS.

### 1.0.638 — 2026-09-05
- تاب مواقيت الصلاة: تسمية زر مصدر الموقع «استخدام موقعي الحالي (GPS)» أصبحت «موقعي الحالي (GPS)».
- نافذة إرشادات معايرة البوصلة: إضافة بند أول «تفعيل موقعك الحالي (GPS) لدقة أفضل.» قبل بندي التحريك على شكل 8 والدوران 360 درجة.

### 1.0.637 — 2026-09-03
- WMM2025: إضافة `tests/wmm-noaa-regression.js` كاختبار regression دائم يشغّل `wmmDeclination()` الفعلية من `prayer.js` مباشرة، بدون نسخ الدالة داخل الاختبار، ويقارنها بقيم NOAA/NCEI الرسمية لـWMM2025 (12 نقطة تشمل 2025.0 و2027.5 وارتفاعي 0 و100 كم).
- الاختبار يتحقق من الانحراف `D` والمجال الكلي `F` بحدّي خطأ `0.01°` و`0.10 nT`، ويفشل إذا تغيّر الكود الفعلي وأصبح خارج المرجع.

### 1.0.515 — 2026-08-25
- تاب الإذاعة: «محمد رفعت» أول محطة في القائمة (الافتراضي).

### 1.0.514 — 2026-08-25
- تاب الإذاعة: إضافة محطة «محمد رفعت» (بث مباشر MP3 من metadata محطة Zeno: `radio.mp3islam.com/listen/refaat/radio.mp3`).

### 1.0.513 — 2026-08-25
- دليل القارئ: إصلاح فعلي لإعادة شريط التابات لموضع البداية عند إعادة الفتح (RTL + ضبط بعد إظهار اللوحة).

### 1.0.512 — 2026-08-25
- دليل القارئ: عند إعادة الفتح من الشاشة الرئيسية يُعاد شريط التابات إلى موضع البداية (علامات الوقف ظاهرة) بدل الإبقاء على موضع التمرير الأفقي السابق بعد زيارة الإذاعة.

### 1.0.511 — 2026-08-25
- تاب الإذاعة: تحسين UX أثناء الاتصال — عند الضغط على تشغيل يظهر فورًا «جاري الاتصال…» مع زر «إيقاف» (بدل بقاء زر تشغيل وإحساس التجمّد). إيقاف أثناء الاتصال يلغي المحاولة فعليًا ويمنع أي حدث playing متأخر من إعادة الحالة إلى بث مباشر. حالات واضحة: idle → connecting → playing.

### 1.0.507 — 2026-08-24
- تاب الإذاعة: إصلاح استئناف التشغيل بعد إيقاف مؤقت — كان الضغط على تشغيل بعد إيقاف الإذاعة والانتظار برهة يُكمل من نفس اللحظة القديمة (buffer المتصفح) بدل نقطة البث الحيّة الفعلية. الآن يُفصَل الاتصال بالكامل عند الإيقاف، فيبدأ الاتصال التالي من اللحظة الحيّة كما يُفترض ببث مباشر.

### 1.0.506 — 2026-08-24
- تاب الإذاعة: إضافة محطة ثالثة «القرآن الكريم من القاهرة» (`https://stream.radiojar.com/8s5u5tpdtwzuv`) إلى قائمة المحطات، بنفس آلية التشغيل (بلا autoplay).

### 1.0.505 — 2026-08-24
- دليل القارئ ← شريط الـTabs: إصلاح تلعثم التمرير الأفقي (كان كاشف السحب الخاص بتبديل التابات يتصادم مع التمرير الأصلي للشريط ويكسر سلاسته عبر `preventDefault`). أُضيف خيار `ignoreTarget` إلى `Gestures.swipe` (اختياري، لا يؤثر على أي استخدام آخر) لاستثناء `.guide-tabs` من كاشف السحب، بالإضافة إلى `touch-action: pan-x` على الشريط.

### 1.0.504 — 2026-08-24
- دليل القارئ: إضافة تاب رابع «الإذاعة» (بجانب علامات الوقف / قصر المنفصل / دعاء الختم) — Radio Player بسيط يعمل داخل التطبيق (بدون iframe أو صفحة خارجية) لمحطتين: مصطفى إسماعيل، ومحمود علي البنا.
- مشغّل الإذاعة مستقل تمامًا عن نظام تشغيل الآيات (`audioManager.js` لم يُمس): عنصر `<audio>` وحالة خاصة به فقط (`radioPlayer`/`radioStream`/`radioStation`/`radioPlaying` في ملف جديد `radio-player.js`). بلا autoplay؛ التشغيل يبدأ فقط بضغط المستخدم، وتبديل المحطة يوقف البث الحالي ويجهّز الجديد دون تشغيل تلقائي، مع حالة واضحة عند تعذّر الاتصال.
- شريط تابات «دليل القارئ» صار قابلًا للتمرير أفقيًا (`overflow-x`) ليستوعب التاب الرابع على الشاشات الضيقة، دون تغيير التصميم الحالي.

### 1.0.503 — 2026-08-22
- دليل القارئ ← علامات الوقف ← رأس الآية: «تُرسَم الدائرة متصلة في صفحة الركوع للدلالة على الوقف لتمام المعنى.»

### 1.0.502 — 2026-08-22
- فهرس المصحف الكامل: رؤوس الأجزاء وحدود السور حسب الجزء الرسمي (`effectiveJuzForPage`) — الجزء 11 يبدأ من التوبة 93 / ع 167 وليس آية 100 / ع 168.

### 1.0.501 — 2026-08-22
- تذييل ع: الربع بأرقام هندية — «نهاية الركوع ١٨٣ • الجزء ١٢ • الربع ١».

### 1.0.500 — 2026-08-22
- تذييل ع: حذف كلمة «رقم» — «نهاية الركوع ١٨٣ • الجزء ١٢ • الربع الأول».

### 1.0.499 — 2026-08-22
- تذييل الصفحة (`.ruku-label`): حجم الخط 13px.

### 1.0.498 — 2026-08-22
- تذييل الصفحة (`.ruku-label`): تكبير الخط من 12px إلى 14px.

### 1.0.497 — 2026-08-22
- تذييل علامة ع: «نهاية الركوع رقم N • الجزء J • الربع …» (مع اسم الربع: الأول…الثامن).

### 1.0.496 — 2026-08-22
- تذييل الصفحة: رقم الجزء من `effectiveJuzForPage` (ع 183 → الجزء 12) بدل `page.juz` فقط.

### 1.0.495 — 2026-08-22
- فهرس الأرباع: الإبراز للربع الذي تقع آية بدايته في الصفحة الحالية (وجهة الضغط على الصف). ع 184 فيها هود 24 → الربع 2، وليس الربع 1 بسبب هود 9.

### 1.0.494 — 2026-08-22
- نطاق العرض = الجزء: حدود السابق/التالي تعتمد على الجزء الرسمي (`effectiveJuzForPage`) وليس `page.juz` فقط — بعد فتح الجزء 12 على ع 183 يمكن الانتقال للصفحة التالية (ع 184) بدل اعتبار ع 183 نهاية الجزء 11.

### 1.0.493 — 2026-08-22
- نطاق الجزء / فهرس الأرباع: الجزء الفعّال للصفحة يُحسب من آية بداية الجزء الرسمية إن وُجدت في الصفحة (ع 183 → جزء 12)، وإلا من موضع أول آية ضمن `RUB_STARTS` — يمنع عرض فهرس الجزء 11 عند فتح الأرباع بعد اختيار الجزء 12.

### 1.0.492 — 2026-08-22
- فهرس الأجزاء + «الذهاب إلى جزء»: بداية الجزء = آية أول ربع فيه (`RUB_STARTS`) على صفحة الركوع التي تحتويها — الجزء 12 → الركوع 183 (هود 6) بدل أول صفحة موسومة juz=12 (ع 184).

### 1.0.491 — 2026-08-22
- فهرس الأرباع (نطاق الجزء): إبراز الربع الحالي حسب أول آية في الصفحة مقابل `RUB_STARTS` وليس فقط نطاق `data-idx` — حتى لا يُبرَز الربع الثاني عند أول صفحات الجزء 12 (ع 184 / هود 9) بينما الآية ما زالت في الربع الأول.

### 1.0.490 — 2026-08-22
- فهرس الأرباع (نطاق العرض = الجزء الحالي): عند اختيار ربع يُفتح الركوع الذي تقع فيه آية بداية الربع الرسمية (`RUB_STARTS`) دون تثبيت داخل صفحات الجزء الموسومة — مثال: الربع الأول من الجزء 12 → الركوع 183 (هود 6).

### 1.0.489 — 2026-08-21
- فهرس الأرباع (نطاق العرض = الجزء الحالي): تثبيت بداية/نهاية كل ربع داخل صفحات الجزء نفسه. كان أول ربع من بعض الأجزاء (مثل الجزء 4 عند آل عمران 93) يقع على صفحة ما زالت موسومة بالجزء السابق، فيخرج التنقّل من النطاق وبعدها يُبرَز الربع الثامن من جزء آخر خطأً.

### 1.0.488 — 2026-08-21
- TTS (أيسر التفاسير + المختصر): تصحيح نطق «يا أيها / ياأيها» — كان يُقرأ بتسكين الياء قبل الهاء؛ أصبح يُنطق يَا أَيُّهَا (تشديد + ضم). النص المعروض دون تغيير.

### 1.0.487 — 2026-08-20
- إصلاح مشكلة Keep Screen Awake (إبقاء الشاشة مضاءة) بعد استعادة الإعدادات من Backup/Restore: بعد الاستعادة لم يكن قفل الشاشة يُعاد تفعيله بشكل صحيح رغم أن الإعداد محفوظ؛ تم تصحيح مسار إعادة التطبيق بعد الاستعادة.

### 1.0.298 — 2026-08-07
- QCF Override: إضافة ﴿ٱلتَّوَّٰبِينَ﴾ (البقرة 222) — Auto Scale فقط.

### 1.0.297 — 2026-08-07
- علامة الإقلاب: وزن أوضح قليلًا (~5–10%) عبر text-stroke رفيع؛ الحجم والموضع واللون دون تغيير.

### 1.0.296 — 2026-08-07
- علامة الإقلاب (`.iqlab-mark`): تكبير ~14% (0.42 → 0.48em) مع الإبقاء على الموضع.

### 1.0.295 — 2026-08-06
- البحث: دعم العبارات العابرة لحدود الآيات المتجاورة (مثل «عوجا قيما» بين 18:1 و 18:2).

### 1.0.294 — 2026-08-06
- البحث: استثناء الحروف المقطَّعة أحادية الحرف (ن/ص/ق) من حدّ الحرفين الأدنى، مع مطابقة تامة تلقائية.

### 1.0.293 — 2026-08-06
- QCF: بناء من مصدر v4 غير ملوّن (جليف أحادي) لإزالة السمك الزائد الناتج عن تراكب طبقات التجويد عند فرض لون موحّد.

### 1.0.292 — 2026-08-06
- QCF Override: إضافة ﴿وَٱلرَّٰسِخُونَ﴾ (آل عمران 7) و﴿ٱلرَّٰسِخُونَ﴾ (النساء 162) — Auto Scale فقط.

### 1.0.291 — 2026-08-06
- دليل القارئ (علامات الوقف): لون نجمة «رأس الآية لغير الكوفيين» في الوضع الليلي = نفس لون باقي الرموز (#F2F2F2).

### 1.0.290 — 2026-08-06
- QCF: Manual scale 0.85 + margin_factor 1.02 لكلمة ﴿الراكعين﴾ (البقرة 43).

### 1.0.289 — 2026-08-06
- QCF Override: لون كلمات الاستبدال في الوضع الليلي = لون نص الآية (#F2F0EA) عبر font-palette.

### 1.0.288 — 2026-08-06
- سمك خط دائرة رأس الآية −10% (2 → 1.8px).

### 1.0.287 — 2026-08-06
- دائرة رأس الآية: نصف القطر 15.

### 1.0.286 — 2026-08-06
- دائرة رأس الآية أكبر بصريًا (r=16)؛ لون رأس غير الكوفيين في الدليل = أخضر العلامة في الفاتحة 7.

### 1.0.285 — 2026-08-06
- دليل القارئ: لون رأس غير الكوفيين بنفس أخضر بقية علامات الدليل.

### 1.0.284 — 2026-08-06
- دليل القارئ: دمج جملة الدائرة في فقرة رأس الآية؛ نقل رأس غير الكوفيين بعد علامة الركوع مع تصغير طفيف.

### 1.0.283 — 2026-08-06
- دليل القارئ: ضبط نص ملحوظة الدائرة؛ رأس غير الكوفيين بلون حبر المصحف؛ دمج جملتي الشرح في فقرة واحدة.

### 1.0.282 — 2026-08-06
- دليل القارئ (علامات الوقف): ملحوظة الدائرة المتصلة في رأس الآية؛ بند جديد لرأس الآية لغير الكوفيين بنفس رمز المصحف.

### 1.0.281 — 2026-08-06
- رقم رأس الآية: إطار **الدائرة** فقط فوق الرقم؛ النجمة تبقى تحت الرقم كما كانت.

### 1.0.280 — 2026-08-06
- رقم رأس الآية: إطار النجمة/الدائرة (`fill=none`) يُرسم فوق الرقم؛ الوسط شفاف فالرقم يبقى ظاهرًا.

### 1.0.279 — 2026-08-06
- تنظيف نهائي: حذف كل أثر لنظام الوقف الهبطي من الكود والتوثيق وسجل التغييرات.

### 1.0.278 — 2026-08-06
- إزالة نظام الوقف الهبطي من التطبيق (عرض وبيانات).

### 1.0.277 — 2026-08-05
- إصلاح رسم دائرة رأس الآية (بدل النجمة) في مصحف النسخ: كانت تُرسم دائرة متصلة لآيات آخر كلمتها تحمل علامة وقف سجاوندي من رموز PUA (ز/ص/ق/قف/وقفة مثل النازعات 27 و29 و31). السبب: منطق الكشف في `tests/no-sajawandi-heads-regression.js` كان يتجاهل رموز PUA كعلامات وقف ويحسب فقط يونيكود السجاوندي المعياري. أُضيفت مجموعة `PUA_WAQF_MARKS` وأُعيد توليد `data/no-sajawandi-heads.json` (4039 موضعًا بدل 4042).

### 1.0.272 — 2026-08-05
- QCF Override: إضافة ﴿بُرَءَٰٓؤُا۟﴾ (60:4) — Auto Scale فقط، بلا Manual Override.


### 1.0.269 — 2026-08-04
- MarkPlacementEngine: مرشّحات 4px مع تفضيل الإزاحة للأعلى قبل الأفقي.

### 1.0.268 — 2026-08-04
- MarkPlacementEngine: خلوص 3px بين النجمة والعوائق (بما فيها `.waqf-sign`).

### 1.0.267 — 2026-08-04
- MarkPlacementEngine: إضافة `.waqf-sign` لعوائق التداخل حتى تتفادى نجمة التذكير علامة المدينة داخل نفس الكلمة (مثل نٓۚ).


### 1.0.263 — 2026-08-04
- QCF: `margin_factor: 1.02` لكلمة ﴿ٱلدَّٰخِلِينَ﴾ (scale 0.80) بعد تداخل بصري مع ﴿مَعَ﴾ عند 1.00.

### 1.0.262 — 2026-08-04
- QCF: `margin_factor: 1.00` موضعي لكلمة ﴿ٱلدَّٰخِلِينَ﴾ (مع الإبقاء على scale 0.80) لتقليل فراغ Auto Margin دون تغيير بقية الكلمات.

### 1.0.261 — 2026-08-04
- QCF: Manual Scale = 0.80 لكلمة ﴿ٱلدَّٰخِلِينَ﴾ (التحريم 10) فقط — Auto Scale ≈0.765 كان غير كافٍ بصريًا؛ بلا تأثير على بقية الكلمات.

### 1.0.234 — 2026-08-02
- الوضع الليلي: رأس الآية ورقمها بلون ورقة المصحف النهاري (`--paper` / #F6F1E4) في **كلا المصحفين** (المدينة والنسخ)، حتى لا يتعارض الأخضر مع تلوين وقف السجاوندي.

### 1.0.233 — 2026-08-02
- الوضع الليلي + مصحف المدينة: رأس الآية ورقمها لم يعودا بالأخضر (#4CAF50) حتى لا يتعارضا مع تلوين وقف السجاوندي الأخضر؛ صار لونهما لون خلفية ورقة المصحف النهاري (`--paper` / #F6F1E4). مصحف النسخ يبقى على الأخضر السابق.

### 1.0.232 — 2026-08-02
- إصلاح تلوين أزرق خاطئ لكلمة «وَٱللَّهُ» في البقرة ٧٢ (مصحف المدينة): الإضافة اليدوية `'2:72:6'` لـTA_MUTLAQ كانت تُطبَّق بعد دمج المسافة الرفيعة داخل «فَٱدَّـٰرَ ٰٔتُمۡ»، فتلوّن الكلمة التالية بدل «فِيهَا». أُزيلت الإضافة الزائدة؛ بقيت `'2:72:5'` التي تُنتج الفهرس الصحيح بعد الدمج.

### 0.9.1 — 2026-07-16
- إضافة `version.js` كمصدر وحيد لرقم الإصدار: يظهر الآن في شاشة الإعدادات/About داخل التطبيق، ويُشتق منه اسم كاش الـ Service Worker (`CACHE = 'juzamma-v' + APP_VERSION`) بدل عدّاد يدوي منفصل (`juzamma-v82`) كان بلا علاقة برقم الإصدار في README. حقل `version` أُضيف أيضًا إلى `manifest.json` مع تحقّق تلقائي (console warning) عند تشغيل التطبيق إن اختلف عن `version.js`.

### 0.9.1 — 2026-07-10
- إنشاء طبقة تخزين مركزية `StorageManager` (في `storage-manager.js`): كل الوصول إلى `localStorage` أصبح حصريًا من خلالها (`loadSettings`/`saveSettings`، `loadLastRead`/`saveLastRead`، `loadFavorites`/`saveFavorites`، `loadBookmarks`/`saveBookmark`، `loadReminder`/`saveReminder`) — لم يعد `app.js` يستدعي `localStorage` مباشرة إطلاقًا.
- إصلاح انفصال علامة الوقف عن حرفها الأساسي في وضع النسخ/إندوباك (PDMS Saleem QuranFont): إزالة تكبير `font-size` عن `.waqf-sign` بعد ثبوت أنه يكسر ارتباط العلامة بحرفها (GPOS) في هذا الخط تحديدًا، مع إبقاء التكبير في وضع مصحف المدينة (Uthmanic Hafs) حيث لا توجد نفس المشكلة.
- إصلاح كسر اندماج "لام-ألف" (لا) قبل علامات الوقف: بعض الكلمات كانت تُفصَل فيها اللام عن الألف التالية بين عنصرين مختلفين في الصفحة، فيفشل اندماجهما البصري (مثال: "مَثَلًا" في البقرة ٢٦).
- إزالة المسافات الصفرية العرض (ZWS) الموجودة في نص textIndopak من مخرجات العرض عند التصاقها بعلامة وقف، دون أي تعديل لملف البيانات الأصلي.
- صفحة جديدة "علامات الوقف والابتداء" — شرح لكل علامات الوقف المستخدمة في التطبيق، بزر وصول سريع من الشاشة الرئيسية.
- استضافة خطي Cairo وAmiri Quran محليًا بدل تحميلهما من Google Fonts — التطبيق الآن مستقل تمامًا عن أي اتصال بالإنترنت حتى في أول تشغيل.
- تجميع كل مفاتيح التخزين المحلي (`STORAGE_KEY`, `FAV_KEY`, `BOOKMARK_KEY`, `WAQF_KEY_LEGACY`) في ملف واحد `constants.js` بدل تفرقتها داخل `app.js`.
- إضافة `id` و`categories` إلى `manifest.json` (أفضل ممارسات PWA).

### قبل بدء هذا السجل
- إصلاح عرض علامة الإقلاب (U+06ED) وعلامة الوقف اللازم (U+06D8) بحيث تُعرَضان بالشكل الصحيح دون المساس بنص القرآن.
- فصل حالة كل مصحف (حجم الخط، العلامات المرجعية، التقدّم، إظهار علامات الوقف) إلى متغيّرات مستقلة لكل من وضعي مصحف المدينة والنسخ، مع منطق ترحيل يحافظ على بيانات المستخدم القديمة.
- إعادة تصميم بطاقات حالة الشاشة الرئيسية، وتعطيل قوائم التحديد الافتراضية عند الضغط المطوّل، وإعادة ترتيب لوحة الإعدادات.

---

---

## خارطة الطريق

- [x] جزء عمّ
- [x] جزء تبارك
- [x] بقية أجزاء القرآن كاملةً (المصحف كله بالركوعات)
- [x] البحث (فهرس السور + بحث بالاسم)
- [x] العلامات المرجعية (المفضلة)
- [x] شاشة رئيسية وشريط تقدّم للقراءة
- [ ] ورد اليوم
- [ ] الإحصائيات
- [ ] التلاوة الصوتية
- [ ] مزامنة البيانات بين الأجهزة

---

## طريقة البناء (Build)

لا يوجد أي خطوة "بناء" (build step) في هذا المشروع — التطبيق كله HTML/CSS/JS عادي بدون أي أداة تجميع (bundler) أو معالج (compiler)، فما تراه في الملفات هو نفسه ما يعمل في المتصفح مباشرة.

**للتشغيل محليًا أثناء التطوير:**
```bash
# أي خادم ثابت (static server) بسيط يكفي، مثلًا:
npx serve .
# أو
python3 -m http.server 8080
```
ثم افتح `http://localhost:<port>` من المتصفح.

> ⚠️ لازم يكون عبر خادم HTTP فعلي، مش عن طريق فتح `index.html` مباشرة من نظام الملفات (`file://`) — الخطوط لا تُحمَّل بشكل صحيح عبر `file://` (انظر "هيكل الملفات" أدناه).

**عند نشر نسخة جديدة (تحديث فعلي يصل للمستخدمين المثبَّت لديهم التطبيق):**
1. عدّل الملفات المطلوبة.
2. **ارفع رقم `CACHE` في `sw.js`** (مثلًا من `juzamma-v24` إلى `juzamma-v25`) — هذه الخطوة إلزامية، وإلا ستستمر النسخة القديمة المخزَّنة (Service Worker) في الظهور للمستخدمين الحاليين حتى لو حدّثتَ الملفات فعليًا على الخادم.
3. إن أضفتَ ملف ثابت جديد (خط، أيقونة) يجب إضافته لقائمة `STATIC_ASSETS` في `sw.js`، أو لقائمة `DYNAMIC_ASSETS` إن كان من الملفات التي تتغيّر مع كل إصدار (HTML/CSS/JS/بيانات).
4. ارفع محتويات المجلد كاملة على الاستضافة (انظر قسم التثبيت أدناه).

لإصدار ملف APK، انظر قسم "لو محتاج ملف APK حقيقي فعلاً" أسفل هذا الملف — لا علاقة له بخطوات "البناء" العادية، فهو اختياري وخاص فقط بمن يريد توزيع التطبيق عبر متجر Android.

---

## الطريقة الأسرع: تثبيته كتطبيق مباشرة (بدون بناء APK)

1. ارفع محتويات هذا المجلد بالكامل على أي استضافة ثابتة (استضافتك، أو GitHub Pages، أو Netlify — مجاني وسريع).
2. افتح الرابط من هاتف Android بمتصفح Chrome.
3. Chrome هيعرض تلقائيًا خيار **"تثبيت التطبيق" / Add to Home screen**.
4. بعد التثبيت، هيظهر التطبيق بأيقونته الخاصة زي أي تطبيق عادي، بشاشة كاملة، ويشتغل أوفلاين.

هذه أسرع وأبسط طريقة، ومفيش أي بناء أو تجميع مطلوب.

---

## لو محتاج ملف APK حقيقي فعلاً

### 1) PWABuilder (الأسهل — من غير أي كود)
1. ارفع المجلد على أي استضافة (نفس الخطوة اللي فوق) عشان يبقى ليه رابط عام.
2. روح على **https://www.pwabuilder.com**
3. حط رابط الموقع، دوسّ Start.
4. اختار Android من قائمة الباكدجات، وحمّل ملف الـ APK/AAB جاهز خلال دقايق.

### 2) Capacitor + Android Studio (لو عايز تحكم كامل في الكود الأصلي)
```bash
npm install @capacitor/core @capacitor/cli @capacitor/android
npx cap init "مصحف الركوع" "com.alnas.mushafruku"
# انسخ محتويات هذا المجلد داخل مجلد www
npx cap add android
npx cap open android
```
وبعدين تبني الـ APK من جوه Android Studio (Build > Build APK) على جهازك اللي عليه Android SDK.

---

## هيكل الملفات
```
index.html      شاشة التطبيق الرئيسية (الرئيسية + القراءة + كل اللوحات)
style.css       التصميم (طراز صفحة مصحف)
constants.js    مفاتيح التخزين المحلي المستخدَمة في كل أنحاء التطبيق
storage-manager.js  الطبقة الوحيدة المسموح لها بالتعامل مع localStorage مباشرة (StorageManager)

app.js          نقطة الدخول: تجميع عناصر DOM (els) وربط كل الوحدات التالية ببعضها (composition root) — لا يحتوي منطق ميزات بنفسه
ui.js           أدوات واجهة عامة مشتركة: تنسيق الأرقام العربية، التوست، فتح/إغلاق اللوحات والنوافذ المنبثقة (بما فيها زر الرجوع في الأندرويد)
dialogs.js      نافذتا "حفظ في المفضلة" و"الذهاب إلى ركوع رقم" — قوالب عامة بترجع القيمة لمن فتحها
home.js         الشاشة الرئيسية: التنقل بينها وبين شاشة القراءة، شريط "استكمال آخر قراءة"، بطاقة "علامة القراءة"
reader-favorites.js  المفضلة: زر النجمة، لوحة المفضلة، الإضافة/الحذف
reader-bookmark.js   علامة القراءة: زر العلامة، بطاقتها في الشاشة الرئيسية
reader-reminders.js  علامات التذكير الشخصية: إضافة/حذف باللمس الطويل، التصدير/الاستيراد
reader-guide.js      لوحة "دليل القارئ" وتبديل تبويبيها (علامات الوقف / دعاء ختم القرآن)
settings.js     حجم الخط، رسم المصحف (عثماني/إندوباك)، الوضع الليلي، إظهار علامات الوقف، التكبير باللمس، قفل الشاشة، تصدير/استيراد العلامات
navigation.js   فهرس الركوعات، فهرس السور، فهرس الأجزاء، البحث، لفتة السحب/التكبير على الصفحة

readerManager.js  تصيير صفحة القراءة نفسها (الآيات وعلامات الوقف والبسملة وشارة السورة) والتنقل بين الصفحات
searchManager.js  فهرس السور وفهرس الآيات القابل للبحث (يُبنى مرة واحدة عند أول استخدام)
audioManager.js   تشغيل الصوت (تلاوة آية/ركوع/سورة كاملة) والتمرير التلقائي المتزامن معه

surah-meta.js             نوع نزول كل سورة (مكية/مدنية) وعدد آياتها
surah-names-vocalized.js  أسماء السور مشكّلة للعرض في الفهرس وشارة بداية السورة (لا تُستخدم في المطابقة/البحث)
juz-info.js     بيانات العنوان والوصف
data.js         نص القرآن الكريم كاملًا مقسّم حسب الركوع (٦٢٣٦ آية / ٥٥٦ ركوعًا)
manifest.json   إعدادات PWA (اسم، أيقونة، ألوان)
sw.js           Service Worker للعمل أوفلاين
fonts/          خطوط المصحف (Uthmanic Hafs, PDMS Saleem) وخطوط الواجهة (Cairo, Amiri Quran) — كلها مستضافة محليًا
icons/          أيقونات التطبيق
```

كل وحدة من `ui.js`/`dialogs.js`/`home.js`/`reader-favorites.js`/`reader-bookmark.js`/`reader-reminders.js`/`reader-guide.js`/`settings.js`/`navigation.js` (وكذلك `readerManager.js`/`searchManager.js`/`audioManager.js`/`storage-manager.js`) تتبع نفس النمط: IIFE مستقلة، دالة `init(deps)` تستقبل بالضبط ما تحتاجه من وحدات تانية، وتصدير واحد محكوم على `window.ModuleName`. ده بيخلي أي تعديل مستقبلي في ميزة معيّنة محصور في ملفها، من غير ما يأثر على باقي التطبيق.

## مصدر البيانات
- نص القرآن: حزمة `quran-json` (نص عثماني موثّق من The Noble Qur'an Encyclopedia / Tanzil).
- حدود الركوعات والأجزاء: حزمة `quran-meta` (بيانات Tanzil metadata الموثّقة لتقسيمات الركوع/الجزء/الحزب).

---

## استبدال كلمات QCF (رسمي)

بعض الكلمات تُعرض برسم مجمع الملك فهد (QCF) بدل KFGQPC لتطابق المصحف
المطبوع بدقة أكبر. القائمة ومواضعها في `tools/qcf-words.json`، والخط
المدمج في `fonts/qcf-merged.woff2`. التفاصيل الكاملة لإضافة كلمة جديدة
في `tools/README.md`.

**خط الأساس:** الإصدار 1.0.248 — راجع `docs/QCF_BASELINE.md`.

**آلية الحجم الرسمية** (ترتيب الأولويات):

1. **Auto Scale** — نسبة عرض KFGQPC ÷ عرض QCF، مقيَّدة ضمن `[0.75, 1.00]`.
2. **Auto Margin** — توسيع المساحة تلقائيًا إن لزم بعد التحجيم (عامل 1.06).
3. **Manual Override** — `scale` / `margin_factor` في `qcf-words.json` فقط
   بعد مراجعة بصرية تُثبت أن التلقائي غير كافٍ.

---

## رؤية المشروع

يهدف "مصحف الركوع" إلى توفير تجربة قراءة تعتمد على الركوع كوحدة مستقلة للورد اليومي، مع واجهة بسيطة، وسرعة عالية، وإمكانية العمل بالكامل دون اتصال بالإنترنت.

وسيتم تطوير التطبيق تدريجيًا حتى يشمل ميزات إضافية تساعد على القراءة والمراجعة والحفظ.
